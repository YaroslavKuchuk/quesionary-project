@model FinalProject.Models.ViewModels.GroupViewModel

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>GroupViewModel</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.BeginingDate)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.BeginingDate)
        </dd>

    </dl>
    <ul>
        <li><h4>@Html.DisplayNameFor(model => model.TeachersList)</h4></li>
        @foreach (var item in Model.TeachersList)
        {
            <li>@item.FirstName  @item.LastName</li>
        }
        <li><h4>@Html.DisplayNameFor(model => model.StudentsList)</h4></li>
        @foreach (var item in Model.StudentsList)
        {
            <li>@item.FirstName  @item.LastName</li>
        }
    </ul>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
